import type { Constellation } from "@/content/types";

export const constellationTextClass: Record<Constellation, string> = {
  signal: "text-signal",
  builder: "text-builder",
  human: "text-human",
};
